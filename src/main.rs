use std::{sync::mpsc, time::{Duration, Instant}, thread};
pub mod cli;
pub mod errors;
pub mod types;
pub mod prelude;
pub mod client;

fn main() -> Result<(), Box<dyn std::error::Error>> {


    Ok(())
}
